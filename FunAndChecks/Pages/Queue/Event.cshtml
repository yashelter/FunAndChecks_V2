@page "/queue/event/{id:int}"
@model FunAndChecks.Pages.Queue.Event
@{
    ViewData["Title"] = $"Очередь: {Model.QueueDetails?.EventName}";
}

@{
    Layout = "_Layout";
}

@if (Model.QueueDetails != null)
{
    <div class="container">
        <h1 class="my-4">@ViewData["Title"]</h1>
        <h5 class="text-muted">@Model.QueueDetails.SubjectName</h5>
        <hr />

        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Статус</th>
                    <th>ФИО</th>
                    <th>Группа</th>
                    <th>Баллы</th>
                    <th>Проверяет</th>
                </tr>
            </thead>
            <tbody id="queue-table-body">
                @foreach (var p in Model.QueueDetails.Participants)
                {
                    <tr id="user-row-@p.UserId">
                        <td id="user-status-@p.UserId">@GetEmojiForStatus(p.Status)</td>
                        <td>@p.FirstName @p.LastName</td>
                        <td>@p.GroupName</td>
                        <td>@p.TotalPoints</td>
                        <td id="user-admin-@p.UserId">@p.CheckingByAdminName</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @* Подключаем скрипты в конце страницы *@
    @section Scripts {
        <script src="~/js/signalr/signalr.min.js"></script>
        <script>
            // Функция-помощник для получения эмодзи
            function getEmojiForStatus(status) {
                switch (status) {
                    case 0: return "⏳"; // Waiting
                    case 1: return "👀"; // Checking
                    case 2: return "✅"; // Finished
                    case 3: return "❌"; // Skipped
                    default: return "⚪️";
                }
            }

            // Создаем подключение к SignalR Hub
            const connection = new signalR.HubConnectionBuilder()
                .withUrl("/queueHub") // URL, который мы указали в Program.cs
                .configureLogging(signalR.LogLevel.Information)
                .build();

            // ID текущего события из модели Razor
            const eventId = @Model.QueueDetails.EventId;

            // Регистрируем обработчик события, которое будет присылать сервер
            connection.on("QueueUserUpdated", (update) => {
                console.log("Update received:", update);

                const userId = update.userId;
                const newStatus = update.newStatus;
                const adminName = update.adminName;

                // Находим элементы в таблице по ID
                const statusCell = document.getElementById(`user-status-${userId}`);
                const adminCell = document.getElementById(`user-admin-${userId}`);

                if (statusCell) {
                    // Обновляем текст ячейки
                    statusCell.textContent = getEmojiForStatus(newStatus);
                }
                if (adminCell) {
                    // Обновляем имя админа (или делаем пустым, если его нет)
                    adminCell.textContent = adminName || "";
                }
            });

            // Функция для старта подключения
            async function start() {
                try {
                    await connection.start();
                    console.log("SignalR Connected.");

                    // После успешного подключения, вызываем метод на сервере для подписки
                    await connection.invoke("SubscribeToQueue", eventId);
                    console.log(`Subscribed to queue ${eventId}`);

                } catch (err) {
                    console.error(err);
                    // Попробовать переподключиться через 5 секунд
                    setTimeout(start, 5000);
                }
            };

            // Обработчик закрытия соединения
            connection.onclose(async () => {
                await start();
            });

            // Запускаем подключение
            start();
        </script>
    }
}
else
{
    <p>Очередь не найдена.</p>
}

@functions {
    // C# функция-помощник для отображения эмодзи при первой загрузке
    private string GetEmojiForStatus(FunAndChecks.Models.Enums.QueueUserStatus status)
    {
        return status switch
        {
            FunAndChecks.Models.Enums.QueueUserStatus.Waiting => "⏳",
            FunAndChecks.Models.Enums.QueueUserStatus.Checking => "👀",
            FunAndChecks.Models.Enums.QueueUserStatus.Finished => "✅",
            FunAndChecks.Models.Enums.QueueUserStatus.Skipped => "❌",
            _ => "⚪️"
        };
    }
}