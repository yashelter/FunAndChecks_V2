@page "/reset-password"
@layout AuthLayout

<PageTitle>@Loc["ResetPassword_PageTitle"]</PageTitle>

<MudPaper Class="pa-8 mx-auto mt-16" MaxWidth="420px" Elevation="4">
    <MudText Typo="Typo.h5" Class="mb-2" Align="Align.Center">@Loc["ResetPassword_Title"]</MudText>
    <MudText Typo="Typo.body2" Class="mb-4" Align="Align.Center">
        @Loc["ResetPassword_Subtitle"]
    </MudText>

    <MudForm @ref="_form">
        <MudTextField @bind-Value="_email" Label="Email" InputType="InputType.Email"
                      Required="true" RequiredError="@Loc["Common_EmailRequired"]" Class="mb-3" />
        <MudTextField @bind-Value="_code" Label="@Loc["ResetPassword_CodeLabel"]"
                      Required="true" RequiredError="@Loc["ResetPassword_CodeRequired"]" Class="mb-3" />
        <MudTextField @bind-Value="_newPassword" Label="@Loc["ResetPassword_NewPasswordLabel"]" InputType="InputType.Password"
                      Required="true" RequiredError="@Loc["ResetPassword_NewPasswordRequired"]" />
    </MudForm>

    @if (_error is not null)
    {
        <MudAlert Severity="Severity.Error" Class="mt-4">@_error</MudAlert>
    }

    <MudButton Variant="Variant.Filled" Color="Color.Primary" FullWidth="true" Class="mt-6"
               OnClick="SubmitAsync" Disabled="_busy">
        @if (_busy) { <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" /> }
        @Loc["ResetPassword_SubmitButton"]
    </MudButton>

    <div class="d-flex justify-center mt-4">
        <MudLink Href="/login">@Loc["ResetPassword_BackToLogin"]</MudLink>
    </div>
</MudPaper>
