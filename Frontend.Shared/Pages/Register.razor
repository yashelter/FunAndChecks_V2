@page "/register"
@layout AuthLayout

<PageTitle>@Loc["Register_PageTitle"]</PageTitle>

<MudPaper Class="pa-8" Elevation="4" Style="width: 100%;">
    <MudText Typo="Typo.h5" Class="mb-4" Align="Align.Center">@Loc["Register_Title"]</MudText>

    <EditForm Model="_model" OnValidSubmit="SubmitAsync">
        <Frontend.Shared.Components.ServerValidator @ref="_serverValidator" />
        <MudGrid Spacing="2">
            <MudItem xs="12" sm="6">
                <MudTextField For="@(() => _model.FirstName)" @bind-Value="_model.FirstName" Label="@Loc["Register_FirstNameLabel"]" Required="true" RequiredError="@Loc["Register_FirstNameRequired"]" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <MudTextField For="@(() => _model.LastName)" @bind-Value="_model.LastName" Label="@Loc["Register_LastNameLabel"]" Required="true" RequiredError="@Loc["Register_LastNameRequired"]" />
            </MudItem>
        </MudGrid>

        <MudTextField For="@(() => _model.Email)" @bind-Value="_model.Email" Label="Email" InputType="InputType.Email"
                      Required="true" RequiredError="@Loc["Common_EmailRequired"]" Class="mt-2" />

        <MudTextField For="@(() => _model.Password)" @bind-Value="_model.Password" Label="@Loc["Register_PasswordLabel"]"
                      InputType="@(_showPassword ? InputType.Text : InputType.Password)"
                      Required="true" RequiredError="@Loc["Common_PasswordRequired"]" Class="mt-2"
                      Adornment="Adornment.End"
                      AdornmentIcon="@(_showPassword ? Icons.Material.Filled.VisibilityOff : Icons.Material.Filled.Visibility)"
                      OnAdornmentClick="@(() => _showPassword = !_showPassword)" AdornmentAriaLabel="@Loc["Register_ShowPassword"]" />

        <MudTextField For="@(() => _model.ConfirmPassword)" @bind-Value="_model.ConfirmPassword" Label="@Loc["Register_ConfirmPasswordLabel"]"
                      InputType="@(_showPassword ? InputType.Text : InputType.Password)"
                      Required="true" RequiredError="@Loc["Register_ConfirmPasswordRequired"]" Class="mt-2"
                      Validation="@(new Func<string, string?>(ValidateConfirm))" />

        <MudSelect T="int" For="@(() => _model.GroupId)" @bind-Value="_model.GroupId" Label="@Loc["Register_GroupLabel"]" Required="true"
                   RequiredError="@Loc["Register_GroupRequired"]" Class="mt-2" Placeholder="@Loc["Register_GroupPlaceholder"]">
            @foreach (var group in _groups)
            {
                <MudSelectItem T="int" Value="group.Id">@group.Name</MudSelectItem>
            }
        </MudSelect>

        @if (_error is not null)
        {
            <MudAlert Severity="Severity.Error" Class="mt-4">@_error</MudAlert>
        }

        <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" FullWidth="true" Class="mt-6" Disabled="_busy">
            @if (_busy) { <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" /> }
            @Loc["Register_SubmitButton"]
        </MudButton>
    </EditForm>

    <div class="d-flex justify-center mt-4">
        <MudLink Href="/login">@Loc["Register_HaveAccount"]</MudLink>
    </div>
</MudPaper>
