@page "/forgot-password"
@layout AuthLayout

<PageTitle>@Loc["ForgotPassword_PageTitle"]</PageTitle>

<MudPaper Class="pa-8 mx-auto mt-16" MaxWidth="420px" Elevation="4">
    <MudText Typo="Typo.h5" Class="mb-2" Align="Align.Center">@Loc["ForgotPassword_Title"]</MudText>
    <MudText Typo="Typo.body2" Class="mb-4" Align="Align.Center">
        @Loc["ForgotPassword_Subtitle"]
    </MudText>

    <MudForm @ref="_form">
        <MudTextField @bind-Value="_email" Label="Email" InputType="InputType.Email"
                      Required="true" RequiredError="@Loc["Common_EmailRequired"]" />
    </MudForm>

    @if (_info is not null)
    {
        <MudAlert Severity="Severity.Success" Class="mt-4">@_info</MudAlert>
    }

    <MudButton Variant="Variant.Filled" Color="Color.Primary" FullWidth="true" Class="mt-6"
               OnClick="SubmitAsync" Disabled="_busy">
        @if (_busy) { <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" /> }
        @Loc["ForgotPassword_SendButton"]
    </MudButton>

    <div class="d-flex justify-center mt-4">
        <MudLink Href="/login">@Loc["Common_BackToLogin"]</MudLink>
    </div>
</MudPaper>
